import { useState } from "react"

export default function DropDownButton(props) {
    const [q,a] = {...props}
    const [currentCollapseState, setCollapseState] =  useState(false);
    const dropDiv =(a) {
        
    }
    return(
        <>
        <div>
            <button onClick={}/>
        </div>
        </>
    )
}